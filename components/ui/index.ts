import Button from "./button";
import { SegmentedTabs } from "./SegmentedTabs";
import { Input, PasswordInput, Select, PhoneNumberInput, TextArea, Checkbox, Radio, Label, ErrorMessage } from "./form";

export {
    Button,
    SegmentedTabs,

    Input,
    PasswordInput,
    Select,
    PhoneNumberInput,
    ErrorMessage,
    TextArea,
    Checkbox,
    Radio,
    Label,
}